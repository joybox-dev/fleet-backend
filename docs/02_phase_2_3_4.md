# Phase 2: New Modules (5-7 days) — ⬜ TODO

All new tables include `company_id` and use `BelongsToCompany` trait.

## 2.1 — Driver Guarantees (2 days)
- Table: `driver_guarantees` — employee_id, guarantee_type (passport/civil_id_copy/etc), document_number, file_path, status (held/returned)
- Model + Controller + Frontend page with create/return modals

## 2.2 — Vehicle Expenses (2 days)
- Table: `vehicle_expenses` — vehicle_id, expense_type (fuel/insurance/tires/etc), amount, expense_date, vendor, receipt_path
- Model + Controller + Frontend page with receipt upload + summary cards

## 2.3 — Salary Advances (2-3 days)
- Table: `salary_advances` — employee_id, amount, monthly_installment, total/paid installments, remaining_balance, status
- Table: `advance_deductions` — tracks each payroll deduction
- Integrate into payroll engine

---

# Phase 3: Operations (5-7 days) — ⬜ TODO

## 3.1 — Attendance System (3-4 days)
- Table: `attendances` — employee_id, attendance_date, status (present/absent/late/excused), check_in/out
- Auto-detection command from daily logs
- Bulk store for marking multiple employees

## 3.2 — Contract Capacity (2-3 days)
- Add `required_drivers` to contracts table
- Operations dashboard: per-contract required vs available vs on_leave vs absent = deficit

---

# Phase 4: HR System (4-5 days) — ⬜ TODO

## 4.1 — Employee Documents (2-3 days)
- Table: `employee_documents` — employee_id, document_type, file_path, expiry_date, status
- Documents tab on Employee Profile with upload per type

## 4.2 — Employee Evaluations (2 days)
- Table: `evaluation_criteria` — configurable rating dimensions per company
- Table: `employee_evaluations` — scores JSON, overall_score, rating
- Evaluation form + history trend chart
